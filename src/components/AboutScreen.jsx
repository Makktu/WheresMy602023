import React from 'react';
import { View, Text, StyleSheet, TouchableOpacity } from 'react-native';

const AboutScreen = () => {
  return (
    <View>
      <Text style={{ color: 'orange' }}>About Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default AboutScreen;

import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const HomeScreen = () => {
  return <Text style={{ fontSize: 30, color: 'orange' }}>Where's My 60?</Text>;
};

const styles = StyleSheet.create({});

export default HomeScreen;
